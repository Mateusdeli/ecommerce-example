import styled from "styled-components";

export const Card = styled.div`
    padding: ${({ theme }) => theme.paddings.medium};

    background-color: #ffffff;

    border-radius: 5px;
    box-shadow: 5px 5px 25px 3px rgba(0, 0, 0, 0.18);
`
